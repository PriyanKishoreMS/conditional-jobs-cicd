# org: priyankishore
app: serverless-extra
service: serverless-extra
frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs18.x
  region: ap-south-1

functions:
  api:
    handler: index.handler
    events:
      - httpApi:
          path: /
          method: get
